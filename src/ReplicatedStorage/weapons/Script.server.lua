--[[DONT DELETE THX THXTHXTXHX

--Offsets for weapons so i dont have to do it again
m4a4 = {
	idle = workspace.Camera.viewmodel.offsets.idle.Value = CFrame.new(0.05,0.2,0.5) * CFrame.Angles(math.rad(2),math.rad(271),math.rad(0))
	aim = default	
}

shotgun = {
	idle = workspace.Camera.viewmodel.offsets.idle.Value = CFrame.new(0.4,-0.3,0.7) * CFrame.Angles(math.rad(2),math.rad(272),math.rad(0)),
	aim = workspace.Camera.viewmodel.offsets.aim.Value = CFrame.new(-0.55,-0.1,0.8) * CFrame.Angles(math.rad(2),math.rad(272),math.rad(0))
	
}

Vandal = {
	 idle = workspace.Camera.viewmodel.offsets.idle.Value = CFrame.new(0.0,0,0.1) * CFrame.Angles(math.rad(-1.25),math.rad(1.4),math.rad(0))
	equip = workspace.Camera.viewmodel.offsets.idle.Value = CFrame.new(0,-0.5,0) * CFrame.Angles(math.rad(0),math.rad(90),math.rad(0))}
--]]
